// let map =new Map()

// //saves the user security stemp inside an array
// function saveUserInfo(securityStamp){
//     userInfo={
//         key:securityStamp.token,
//         value:securityStamp
//     }
//     map.set(userInfo.key,userInfo.value);
// }

// //checks if the toke of the user is inside the array and return its id
// function checkMapForUserId(token){
//     let loginWithoutBearer = token.substring("Bearer ".length);
//     let userDetails = map.get(loginWithoutBearer);
//     return userDetails

// }


// module.exports = { saveUserInfo, checkMapForUserId }